import React from "react";
import { Button } from "./common/Button";

const Hero = () => {
  return (
    <div>
      <Button customCss="">talk to allan</Button>
    </div>
  );
};

export default Hero;
